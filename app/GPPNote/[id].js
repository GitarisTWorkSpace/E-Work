import { StyleSheet} from 'react-native';
import React, { useState } from 'react';
import GppNote from '../../components/GPPNote/GppNote';


const TPNote = () => {
    return (
        <GppNote />
    )
}

export default TPNote;

const styles = StyleSheet.create({})