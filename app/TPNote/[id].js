import { StyleSheet} from 'react-native';
import React, { useState } from 'react';
import TpNote from '../../components/TPNote/TpNote';

const TPNote = () => {
    return (
        <TpNote />
    )
}

export default TPNote;

const styles = StyleSheet.create({})